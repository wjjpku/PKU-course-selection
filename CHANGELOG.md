# 更新记录

这里记录本地工作台的发布版本；原选课引擎的历史版本记录仍保留在 `skj包/HISTORY.md`，两者独立编号。

## 未发布 — 可信数据与任务配置一致性

- 课程卡片分别展示查询时间、学期、上课时间识别程度；文件更新时间不再冒充查询时间。旧快照、同步失败及缺失信息明确提示。
- 任务显示结构化原因、处理建议、等待状态；提交后必须复核，不将提交成功或互斥跳过显示为已经选上。
- 每个任务具有配置版本。启动和修改必须提交确认过的版本，不一致返回 409，且不会启动或覆盖配置；暂停、停止不受版本限制。
- 保存的参数按实际执行值归一化，展示本次/上次运行版本；轮询日志记录配置版本，重启后仍停止，保留历史版本来源。
- 52 项 Python 测试、4 组前端检查通过；独立示例浏览器验证了旧弹窗启动拦截、原因提示、展开状态保持与手机布局。
- 本次只修改本地源码与展示构建，不重启现有服务，不发布 GitHub / Vercel，不执行真实选课或退课。

升级生效时，先手动停止任务，再重启后端并刷新网页。旧前端无法启动新版后端任务，新前端也不会在缺少版本校验的旧后端上启动任务。不要在正在刷课时直接重启升级。

## 0.1.0 — 2026-09-07

首个工作台源码发布。

### 查询与规划

- 按当前账号可见范围查询课程，遍历学校表单的分类和分页，导出 UTF-8 BOM CSV。
- 接入本地课程库，支持关键词、查询入口类型、院系、学分和年级组合筛选；按账号记住条件和页码。
- 同一记录的多个入口合并展示、保留原始来源；导出全部筛选结果而非仅当前页。
- 课程卡片展示上课时间、P/NP、余量快照及冲突课程、节次和教学周；无时间课程保留并标明无法确认。
- 课程篮与课表预览独立于真实选课；提供浅色、深色、跟随系统主题和移动端布局。

### 运行与持久化

- 独立刷课任务、可调参数和最近 100 条轮询日志，记录未选中的原因。
- SQLite 保存任务、日志、课程缓存及最近可用课程库；重启恢复但不自动开始选课。
- 全量更新失败且没有结果时，不清空之前可用的课程库。
- 保留本地 OCR、默认仅本机访问和明确启动操作；不提供退课或自动换课功能。

### 开发与发布

- 新增不需要真实账号的离线回归入口 `scripts/check.py`。
- 提供最新使用说明、贡献指南、版本记录及发布说明；保留原作者许可与借鉴来源。
- 发布内容不包含账号配置、个人数据库、课程 CSV、浏览器资料或日志。

### 验证与限制

- 本机 macOS / Python 3.12：46 项 Python 测试、3 组前端逻辑检查及 JavaScript 语法检查通过。
- 隔离浏览器模拟数据验证：筛选与页码恢复、卡片冲突说明、课程篮、跨页 CSV 导出和移动端布局。
- 本地已有课程快照成功载入；这不证明所有账号、学期或选课阶段都兼容。
- 没有用真实选课或退课验证本版本；是否选课成功须以学校最终结果为准。
