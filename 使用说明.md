# 选课工作台

双击「启动工作台.command」，访问 http://127.0.0.1:7074。
当前独立环境包含页面、选课客户端和已实测的本地验证码依赖。

1. 在「账号与会话」保存账号。留空保留已有值，密码不会回显。
2. 在「课程计划」编辑课程与优先级，在「规则与频率」调整互斥组、阈值、页码与超时。
3. 点击「保存并应用」后配置写入后端；浏览器草稿不会自行影响任务。
4. 「登录检查」只验证登录；「开始运行」会访问补退选页面并尝试补选。学校返回非操作时段时停止。
5. 暂停、继续、停止在页面操作。当前请求最多等待配置的超时时间后停止，不会强行中断正在提交的请求。
6. 「测试本地验证码」识别历史 20 张图片，页面显示正确数和最近一次识别结果；不会访问学校。

页面每两秒刷新运行状态、课程余量、提交次数、最近验证码及事件。
页面任务采用单会话顺序执行。旧命令行会话池设置仅保留兼容用途。
退出会话会清除本地 Cookie。账号仍以明文保存在本机 config.ini，写入权限为仅当前用户可读写。
真实课程结果以学校返回列表复核；当前尚未在开放的补退选时段验证线上协议。
