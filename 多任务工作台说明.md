# 多任务工作台

## 使用顺序

1. 首页为选课页；需要学校数据时登录并读取课程。
2. 将课程加入页面下方的课程篮，在“新任务参数与课程规则”中调整该组参数。
3. 点击“创建刷课任务”。创建只生成待启动任务，不提交选课，也不修改原账号配置。
4. 在“刷课”目录逐个核对并启动任务；每项任务独立显示循环、课程结果和日志，并可暂停、继续、停止。
5. 停止后可调整该任务的基础等待间隔，或移除本地任务。移除本地任务不等于学校退课。

## 运行边界

- 每个任务使用独立线程、配置快照、状态、日志和选课会话；不是额外启动一个操作系统进程。
- 同时运行最多 3 项，保留最多 20 项。所有学校请求经过一个共享限速器，前一个请求结束后至少等待 4 秒。
- 同一课程（名称、班号、开课单位一致）不能加入多个任务。
- 任务运行时阻止账号改动、退出登录和独立读取课程，避免影响运行会话；仍可创建新的待启动任务。
- 暂停和停止无法撤回已发出的请求，在请求边界生效。
- 任务列表、任务日志仅保存在服务内存中，服务重启后丢失；不会自动恢复或自动启动。
- 本次验证采用离线和模拟测试；真实多任务补选成功率与学校多会话兼容性尚未验证。没有退课功能。

## 系统调试

可逐项或一键运行：

- 任务、配置与接口回归：包含配置回读、保存回滚、权限、课程解析、重复课程拒绝、独立暂停/停止与共享限速等 23 项测试。
- 本地验证码历史样本：独立加载识别器测试 20 张历史图片，不请求学校验证码。

每组测试在单独的 Python 进程中运行，禁用 Python socket 网络连接，最长 90 秒。运行任务的状态、会话和配置不会传入测试进程。测试会短暂使用本机 CPU，不能保证完全没有资源开销。

真实登录、课程读取不属于离线自检，需要从选课页面明确触发。
